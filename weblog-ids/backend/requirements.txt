# WebLog-IDS - Backend dependencies
# Python 3.10+

# Web framework & ASGI server
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Data validation
pydantic==2.7.4

# Data processing (statistik, CSV export, evaluasi)
pandas==2.2.2

# Evaluasi metrik (accuracy, precision, recall, f1)
scikit-learn==1.5.0

# WebSocket support untuk uvicorn[standard] sudah termasuk (websockets)
# SQLite sudah bawaan Python (modul sqlite3), tidak perlu paket tambahan
